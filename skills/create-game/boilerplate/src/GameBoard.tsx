// REPLACE: build the game board here

interface Props {
  // REPLACE: add game state and action props from useGame
}

export default function GameBoard({}: Props) {
  return <div className="game-board">{/* REPLACE: render the game board */}</div>;
}
